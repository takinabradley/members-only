<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Members Only Login</title>
</head>
<body>
  <h1>Register In</h1>
  <p>or <a href="/login">login</a></p>
  <form action="/register" method="POST">
    <label>
      Username:
      <input type="text" pattern="[\w!@#$%^&\*]*" minlength="3" name="username" required>
    </label>

    <label>
      Password:
      <input type="password" pattern="[\w!@#$%^&*]*" minlength="8" name="password" required>
    </label>

    <label>
      Confirm Password:
      <input type="password" pattern="[\w!@#$%^&*]*" minlength="8" name="confirmPassword" required>
    </label>
    
    <button type="submit">Submit</button>
  </form>
  <div class="errors">
    <% errors.forEach(err => { %>
      <p><%= err.msg %></p>
    <% }) %>
  </div>
</body>
</html>